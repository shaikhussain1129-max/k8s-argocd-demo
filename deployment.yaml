apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-nginx
spec:
  replicas: 4
  selector:
    matchLabels:
      app: argocd-nginx
  template:
    metadata:
      labels:
        app: argocd-nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
